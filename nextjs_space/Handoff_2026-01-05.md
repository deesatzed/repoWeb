# DevShowcase / RepoNexus — Engineering Handoff Packet (as of 2026-01-05)

## 1. Executive Snapshot (1 page max)

### Current development phase
- Stabilization + workflow gating work for the portfolio curation pipeline (login → connect GH → sync → curate/filter → (manual group OR auto-group) → revise → AI analysis → employer view).
- Focus areas in the current session:
  - Prevent unintended auto-curation / unexpected step jumps.
  - Add dry-run preview before DB mutation.
  - Fix local dev reliability: stale session FK errors, client-side crashes, stuck spinners.

### Last stable commit (SHA + date)
- `main#c2494d59a71b6168a616905990667fb205c6100d` (2026-01-04T20:17:56-05:00)
  - Message: `feat: add nuclear portfolio reset`

### Working tree state (important)
- **Uncommitted changes exist** across multiple files (API routes, UI components, Prisma schema, lockfiles).  
  TODO: commit SHA/date for current working changes (uncommitted as of 2026-01-05; source: `git status --porcelain`).

### What is working vs. incomplete
- Working:
  - Local workflow baseline can be brought to a non-500 state after signing up / signing in and connecting GitHub (observed via dev logs).
  - GitHub sync endpoint completes and updates repository data (observed in dev logs).
  - Auto-curate endpoint requires explicit intent and supports preview/apply separation.
  - Repository list no longer crashes when `citations` is not an array (UI-side normalization).
- Incomplete / still problematic:
  - Workflow gating remains confusing: “Smart Grouping started before curating” user complaint. Needs explicit state machine / gating instead of inferred counts.
  - Repo evidence file fetching can fail due to incorrect owner/repo parsing (leads to GitHub `/contents` 404s).
  - Repository analysis SSE stream can error with `ERR_INVALID_STATE: Controller is already closed` (observed in server logs; requires fix).
  - Dependency manager drift: both `yarn.lock` and `package-lock.json` exist; `npm install` required `--legacy-peer-deps` due to ESLint peer conflicts.

### 1-Day Resume Checklist (Required)
- [ ] Confirm current branch + clean/dirty state (`git rev-parse --abbrev-ref HEAD`, `git status --porcelain`).
- [ ] Local dev: start server, verify `GET /api/portfolio/settings`, `GET /api/github/connect`, `GET /api/portfolio/stats` are not 500.
- [ ] Run tests: `npm test` (or `yarn test` if migrated).
- [ ] Reproduce “Smart Grouping started early” report and confirm whether `POST /api/curate/auto` fires without click (DevTools Network filter `curate`).
- [ ] Fix GitHub repo file evidence fetching (owner/repo parsing) and re-test repository analysis citations.
- [ ] Commit + push stabilization changes; capture commit SHA/date and update this handoff.

### Active owners
- **TL:** TODO: owner?, due?, source?
- **PM:** TODO: owner?, due?, source?
- **DevOps:** TODO: owner?, due?, source?
- **QA:** TODO: owner?, due?, source?
- **Security:** TODO: owner?, due?, source?

---

## 2. Project Specification (What)

### Scope & goals (current)
- Provide a web UI for a developer to:
  - Authenticate.
  - Connect GitHub (token or OAuth).
  - Sync repositories into a local DB.
  - Curate by excluding low-value repos.
  - Organize repos into projects (manual or AI-assisted).
  - Run AI analyses for repos and projects and render employer-facing views.

Evidence:
- Local startup + workflow pages: `docs/STARTUP.md:L57–L63 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Non-goals
- TODO: explicitly documented non-goals not found in repo docs (source?).

### Stakeholders and requirements
- User requirement (as expressed in the active session): strict gating and explicit intent per workflow step; prevent unintended AI auto-curation.
  - TODO: capture in an issue/ADR (owner?, due?, source?).

### Success metrics with current baselines
- TODO: baseline metrics not present (e.g., sync time, analysis success rate, conversion to employer-ready page).

---

## 3. Implementation State (How)

### Module/component responsibilities (selected)
- **Auth (NextAuth + Prisma adapter)**
  - `lib/auth-options.ts` defines providers + callbacks; credentials provider always enabled.
  - Evidence: `lib/auth-options.ts:L1–L169 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- **DB access (Prisma)**
  - Singleton Prisma client in `lib/db.ts`.
  - Evidence: `lib/db.ts:L1–L12 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- **GitHub API integration**
  - `lib/github-api.ts` wraps Octokit; decrypts stored token, fetches repos, languages, readme, sample files.
  - Evidence: `lib/github-api.ts:L1–L166 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- **LLM integration (OpenRouter via OpenAI-compatible SDK)**
  - `lib/llm.ts` provides `analyzeText` and `analyzeJSON` using baseURL `https://openrouter.ai/api/v1`.
  - Evidence: `lib/llm.ts:L1–L74 @ main (TODO: commit SHA/date; currently modified in working tree)`
- **Auto-curation (AI grouping + exclusion)**
  - API route `app/api/curate/auto/route.ts` supports `mode: preview|apply` and requires `intent: manual`.
  - Evidence: `app/api/curate/auto/route.ts:L22–L173 @ main (TODO: commit SHA/date; currently modified in working tree)`
- **Repository analysis with citations (SSE)**
  - API route `app/api/analyze/repository/route.ts` streams progress and saves analysis, including `citations` stored as JSON string.
  - Evidence: `app/api/analyze/repository/route.ts:L13–L259 @ main (TODO: commit SHA/date; currently modified in working tree)`
- **Project analysis (SSE)**
  - API route `app/api/analyze/project/route.ts` streams progress and stores project-level analysis.
  - Evidence: `app/api/analyze/project/route.ts:L10–L215 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- **Curation UI**
  - `app/dashboard/_components/portfolio-curation.tsx` loads repos/projects, handles auto-curate preview/apply.
  - Evidence: `app/dashboard/_components/portfolio-curation.tsx:L63–L146 @ main (TODO: commit SHA/date; currently modified in working tree)`

### Current strategy and boundaries
- Server routes perform all DB mutation and LLM calls.
- Client components fetch API routes and display toasts/spinners.
- `server-only` guard is used for LLM wrapper to prevent client bundling.
  - Evidence: `lib/llm.ts:L1–L4 @ main (TODO: commit SHA/date; currently modified in working tree)`

### Dependency graph (ASCII)

```
UI (Dashboard / Curation)
  -> fetch /api/github/connect
  -> fetch /api/github/sync
  -> fetch /api/repositories, /api/projects
  -> fetch /api/curate/auto (manual intent + preview/apply)
  -> fetch /api/analyze/repository (SSE)
  -> fetch /api/analyze/project (SSE)

/api/*
  -> prisma (SQLite)
  -> GitHubService (Octokit + decrypt)
  -> analyzeJSON (OpenRouter)
```

### Open design decisions (ADRs)
- TODO: ADRs not found in repo (owner?, due?, source?).

---

## 4. Architecture & Environments

### High-level architecture
- Next.js web app (server + client components)
- Prisma ORM
- SQLite datastore
- External services:
  - GitHub API via Octokit
  - OpenRouter (OpenAI-compatible API)

### Environment overview
- **Local Dev**: SQLite file in repo; env from `.env`.
  - Evidence: `docs/STARTUP.md:L31–L49 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- **Prod (Fly.io)**:
  - App: `reponexus-portfolio`.
  - SQLite volume mounted at `/data`, `DATABASE_URL=file:/data/sqlite.db`.
  - Evidence: `fly.toml:L6–L31 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### URLs
- Prod URL mentioned in docs: `https://reponexus-portfolio.fly.dev/`
  - Evidence: `docs/DEPLOYMENT_FLY.md:L77–L80 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Networking
- Fly service internal port 3000.
  - Evidence: `fly.toml:L11–L18 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

---

## 5. Versions & Compatibility

### Languages/SDKs
- Node.js 20.x (doc claim)
  - Evidence: `docs/STARTUP.md:L7–L9 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Frameworks/libraries
- Next.js: `14.2.28`
  - Evidence: `package.json:L100–L102 @ main (TODO: commit SHA/date; working tree modified)`
- Prisma: `6.7.0`
  - Evidence: `package.json:L28–L29 @ main (TODO: commit SHA/date; working tree modified)`
- NextAuth: `4.24.11`
  - Evidence: `package.json:L100–L102 @ main (TODO: commit SHA/date; working tree modified)`

### Container images
- Base image: `node:20-alpine`
  - Evidence: `Dockerfile:L1–L2 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Datastores
- SQLite via Prisma datasource
  - Evidence: `prisma/schema.prisma:L6–L9 @ main (TODO: commit SHA/date; working tree modified)`

### Compatibility matrix (Required)

| Component | Version | Depends On | Compatible With | Notes |
| --------- | ------- | ---------- | --------------- | ----- |
| Node | 20.x | - | Next.js 14 | Doc states Node 20.x (`docs/STARTUP.md`). |
| Next.js | 14.2.28 | React 18 | - | Package pins Next 14.2.28. |
| Prisma | 6.7.0 | SQLite | - | SQLite DB local + Fly volume. |
| LLM | OpenRouter API | OPENROUTER_API_KEY | OpenAI SDK | Base URL `https://openrouter.ai/api/v1`. |

---

## 6. Build & Run Guides

### Local dev setup
- Install deps (recommended path is unclear due to dual lockfiles):
  - Option A (doc): `yarn --frozen-lockfile`
  - Option B (observed necessary): `npm install --legacy-peer-deps` due to ESLint peer conflicts.
  - TODO: pick one package manager and remove the other lockfile.

Evidence:
- Startup doc references Yarn classic: `docs/STARTUP.md:L7–L9 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- Dockerfile supports yarn/npm based on lockfile presence: `Dockerfile:L10–L16 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Database setup
- `npx prisma generate`
- `npx prisma db push`

Evidence:
- Prisma client generate during Docker build: `Dockerfile:L30–L37 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- Local setup steps: `docs/STARTUP.md:L31–L39 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Running locally
- `npm run dev` (script)
  - Evidence: `package.json:L4–L9 @ main (TODO: commit SHA/date; working tree modified)`

### Common pitfalls + evidence
- **Stale session FK failures (P2003)** when DB user row is missing but NextAuth session exists.
  - Mitigation implemented: routes check `prisma.user.findUnique({ id: session.user.id })` and return 401 with dev details.
  - Evidence: `app/api/github/connect/route.ts:L18–L33 @ main (TODO: commit SHA/date; working tree modified)` and `app/api/portfolio/settings/route.ts:L16–L31 @ main (TODO: commit SHA/date; working tree modified)`
- **Citations field type drift** (string vs array) caused UI crash.
  - Mitigation implemented in UI by normalizing/parsing.
  - Evidence: `app/dashboard/_components/repository-list.tsx:L573–L616 @ main (TODO: commit SHA/date; working tree modified)`
- **Repo file fetching for analysis likely broken**: owner/repo extracted from `repository.name` via `split('/')`.
  - Risk: `repository.name` appears to be repo short name (no `/`), causing blank owner and GitHub 404.
  - Evidence: `app/api/analyze/repository/route.ts:L72–L76 @ main (TODO: commit SHA/date; working tree modified)`

### Local Dev Sanity Checklist (Required)
- [ ] `npm run dev` starts and `/dashboard` loads.
- [ ] `GET /api/portfolio/settings` returns 200 (not 500).
- [ ] `GET /api/github/connect` returns 200.
- [ ] `POST /api/github/sync` returns 200.
- [ ] No unexpected `POST /api/curate/auto` without explicit click.

---

## 7. Deployment & Release

### Branching & promotion flow
- Default branch observed: `main`.
  - Evidence: `git rev-parse --abbrev-ref HEAD` output (local).
  - TODO: confirm team policy (trunk vs gitflow) and CI gates.

### Fly.io deployment
- Deploy command: `fly deploy`
  - Evidence: `docs/DEPLOYMENT_FLY.md:L52–L58 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- Persistent SQLite volume and env:
  - Evidence: `fly.toml:L24–L31 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Rollback/hotfix
- TODO: rollback procedure not documented beyond Fly tooling (owner?, due?, source?).

### Pre-Deploy Gate Checklist (Required)
- [ ] `npm test` passes.
- [ ] Local smoke: login, connect GitHub, sync, curate, grouping preview/apply, analysis.
- [ ] Secrets configured in Fly (`fly secrets set ...`).
- [ ] Prisma schema up to date on Fly volume (`fly ssh console` → `npx prisma db push`).

---

## 8. Configuration, Secrets & Compliance

### Config surfaces
- `.env` for local.
  - Do not commit.
  - Evidence: `docs/STARTUP.md:L13–L16 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- Fly secrets for production.
  - Evidence: `docs/DEPLOYMENT_FLY.md:L34–L46 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Secrets handling patterns (redacted)
- Required env vars (do **not** store in git):
  - `NEXTAUTH_SECRET`
  - `ENCRYPTION_KEY` (32 bytes in hex; used for AES-256-CBC)
    - Evidence: `lib/encryption.ts:L3–L16 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
  - `OPENROUTER_API_KEY` (or `OPENAI_API_KEY` fallback)
    - Evidence: `lib/llm.ts:L13–L21 @ main (TODO: commit SHA/date; working tree modified)`
  - OAuth:
    - `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET` (optional; providers enabled only if not placeholder)
      - Evidence: `lib/auth-options.ts:L10–L40 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

#### Provisioning steps
- Fly:
  - `fly secrets set NEXTAUTH_SECRET=... ENCRYPTION_KEY=... OPENROUTER_API_KEY=... GITHUB_CLIENT_ID=... GITHUB_CLIENT_SECRET=...`
  - Evidence: `docs/DEPLOYMENT_FLY.md:L36–L46 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

#### Rotation policy
- TODO: define rotation policy for `ENCRYPTION_KEY`, `NEXTAUTH_SECRET`, and API keys (owner?, due?, source?).

### Secrets & Compliance Checklist (Required)
- [ ] `.env` not committed.
- [ ] Fly secrets set via `fly secrets set`.
- [ ] Encryption key length/format validated.
- [ ] No tokens logged to console/server logs.

---

## 9. Testing, QA & Quality Gates

### Test coverage snapshot
- Unit tests exist under `tests/*.test.ts` and run via Node test runner.
  - Evidence: `package.json:L4–L9 @ main (TODO: commit SHA/date; working tree modified)`
- Observed result in session: `20` tests passing.  
  TODO: capture CI evidence and coverage % (owner?, due?, source?).

### Local test instructions
- `npm test`
  - Evidence: `package.json:L4–L9 @ main (TODO: commit SHA/date; working tree modified)`

### Linters/quality gates
- ESLint configured, but dependency conflicts observed with ESLint 9 vs TS-ESLint peer ranges.
  - Evidence: `package.json:L20–L25 @ main (TODO: commit SHA/date; working tree modified)`

---

## 10. Observability & Operations

### Logs
- Fly logs:
  - `fly logs`
  - Evidence: `docs/DEPLOYMENT_FLY.md:L83–L90 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Dashboards/alerts
- TODO: none referenced in repo (owner?, due?, source?).

### Runbooks
- TODO: add runbook for:
  - GitHub sync failures
  - OpenRouter failures/timeouts
  - Prisma/SQLite volume issues

---

## 11. Work Stream & Next Steps

### Top 5 next steps

1) **Goal:** Fix workflow gating so Smart Grouping cannot start before explicit curate completion  
   **Why now:** User trust + repeated “started early” complaint.  
   **Owner:** TODO: owner?  
   **How:** introduce explicit state machine / persisted step completion instead of inferred counts; ensure UI does not show grouping CTA until curate step confirmed.  
   **Done when:** No `POST /api/curate/auto` without click; UI step progression is deterministic.

2) **Goal:** Fix owner/repo parsing for repository file evidence fetching  
   **Why now:** citations + analysis quality depend on it; current logic likely uses wrong field.  
   **Owner:** TODO: owner?  
   **How:** use `repository.fullName` or store owner separately; update `getRepositoryFiles()` call inputs.  
   **Done when:** GitHub `/contents` requests succeed and citations align with real files.

3) **Goal:** Fix SSE controller invalid state error in repository analysis route  
   **Why now:** breaks analysis streaming reliability.  
   **Owner:** TODO: owner?  
   **How:** ensure controller is not closed twice; guard writes after close; handle aborts.  
   **Done when:** repeated analyses do not emit `ERR_INVALID_STATE`.

4) **Goal:** Resolve dependency manager drift (yarn vs npm) + peer conflicts  
   **Why now:** reproducibility + onboarding.  
   **Owner:** TODO: owner?  
   **How:** pick yarn or npm, remove the other lockfile, align eslint + @typescript-eslint versions.  
   **Done when:** clean install without flags.

5) **Goal:** Formalize “Engineering Handoff Packet” automation  
   **Why now:** repeated context loss and long debugging sessions.  
   **Owner:** TODO: owner?  
   **How:** add script to compile metadata (git SHA, status, key endpoints) and template doc.  
   **Done when:** can regenerate packet in <5 minutes.

### Blocked items & dependencies
- TODO: track in issue tracker / GitHub Projects (owner?, due?, source?).

### Risk matrix
- Risk: workflow auto-triggers or appears to auto-trigger → loss of user trust.
- Risk: analysis citations incorrect → undermines employer-facing credibility.
- Risk: dependency install unreproducible → slows onboarding.

### Decision log / ADR index
- TODO: not found.

---

## 12. Appendices

### API surfaces (endpoints + examples)

- `POST /api/signup` (create credentials user)
  - TODO: document request/response schema (source?).

- `POST /api/github/connect`
  - Body: `{ "githubToken": "<token>" }` (token encrypted server-side)
  - Evidence: `app/api/github/connect/route.ts:L35–L77 @ main (TODO: commit SHA/date; working tree modified)`

- `POST /api/github/sync`
  - Syncs repos from GitHub.
  - Evidence: `app/api/github/sync/route.ts:L9–L123 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

- `POST /api/curate/auto`
  - Body (preview): `{ "intent":"manual", "mode":"preview" }`
  - Body (apply): `{ "intent":"manual", "mode":"apply", "plan": { ... } }`
  - Evidence: `app/api/curate/auto/route.ts:L30–L173 @ main (TODO: commit SHA/date; working tree modified)`

- `POST /api/analyze/repository` (SSE)
  - Body: `{ "repositoryId": "<id>" }`
  - Evidence: `app/api/analyze/repository/route.ts:L13–L29 @ main (TODO: commit SHA/date; working tree modified)`

- `POST /api/analyze/project` (SSE)
  - Body: `{ "projectId": "<id>" }`
  - Evidence: `app/api/analyze/project/route.ts:L10–L26 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

- `POST /api/portfolio/reset` ("NUKE" reset)
  - Body: `{ "confirm": "NUKE" }`
  - Evidence: `app/api/portfolio/reset/route.ts:L6–L99 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`

### Data model (structured list)
- SQLite schema via Prisma models:
  - `User`, `Account`, `Session` (NextAuth)
  - `GitHubConnection` (1:1 with User)
  - `Repository` (many per GitHubConnection)
  - `AIAnalysis` (1:1 with Repository; citations stored as JSON string)
  - `Project` (many per User; has many Repositories)
  - `ProjectAnalysis` (1:1 with Project)
  - `PortfolioSettings` (1:1 with User)

Evidence:
- `prisma/schema.prisma:L37–L159 @ main (TODO: commit SHA/date; working tree modified)`

### Migrations
- Prisma uses `db push` (schema sync) rather than tracked migrations in the referenced docs.
  - Evidence: `docs/STARTUP.md:L35–L39 @ main#c2494d59a71b6168a616905990667fb205c6100d (2026-01-04T20:17:56-05:00)`
- TODO: identify current migration head / introduce migrations if needed.

### Glossary
- **Curation**: selecting which repos are visible and how they’re grouped.
- **Auto-curate**: LLM-generated plan to exclude repos and group repos into projects.
- **Preview plan**: dry-run output; no DB mutation.
- **Apply plan**: executes exclusion + grouping in DB.

---

```json
{
  "project_name": "DevShowcase / RepoNexus",
  "pause_date_iso": "2026-01-05",
  "repo_roots": ["/Volumes/WS4TB/repoWeb/github_portfolio_showcase/nextjs_space"],
  "default_branch": "main",
  "last_stable_commit": {
    "sha": "c2494d59a71b6168a616905990667fb205c6100d",
    "date": "2026-01-04T20:17:56-05:00",
    "message": "feat: add nuclear portfolio reset"
  },
  "modules": [
    {
      "name": "NextAuth auth options",
      "path": "nextjs_space/lib/auth-options.ts",
      "owner": "TODO: owner?",
      "status": "yellow",
      "notes": "Credentials provider always enabled; OAuth providers enabled only if env vars are not placeholders."
    },
    {
      "name": "GitHub integration (Octokit)",
      "path": "nextjs_space/lib/github-api.ts",
      "owner": "TODO: owner?",
      "status": "yellow",
      "notes": "Repo file evidence fetching may be broken due to owner/repo parsing in analyze route; needs fix."
    },
    {
      "name": "LLM integration (OpenRouter)",
      "path": "nextjs_space/lib/llm.ts",
      "owner": "TODO: owner?",
      "status": "yellow",
      "notes": "Server-only wrapper around OpenAI SDK; requires OPENROUTER_API_KEY."
    },
    {
      "name": "Auto-curate API",
      "path": "nextjs_space/app/api/curate/auto/route.ts",
      "owner": "TODO: owner?",
      "status": "yellow",
      "notes": "Supports preview/apply and requires explicit intent; ensure workflow gating aligns with UI."
    },
    {
      "name": "Repository analysis API (SSE)",
      "path": "nextjs_space/app/api/analyze/repository/route.ts",
      "owner": "TODO: owner?",
      "status": "red",
      "notes": "Observed controller invalid state error; evidence file fetching likely wrong."
    }
  ],
  "versions": {
    "languages": [{"name": "node", "version": "20.x"}],
    "frameworks": [
      {"name": "next", "version": "14.2.28"},
      {"name": "next-auth", "version": "4.24.11"},
      {"name": "prisma", "version": "6.7.0"}
    ],
    "containers": [
      {"image": "node", "tag": "20-alpine", "digest": "<TODO: sha256 digest from build output>"}
    ],
    "datastores": [
      {"engine": "sqlite", "version": "(via prisma)", "migration_head": "(db push; TODO)"}
    ]
  },
  "ci_cd": {
    "pipelines": [{"name": "TODO", "status": "failing", "url": "TODO"}],
    "release_flow": "other"
  },
  "environments": [
    {"name": "dev", "url": "http://localhost:3000", "parity_gaps": ["Dependency manager drift (yarn vs npm)", "Stale session FK failures when DB reset"]},
    {"name": "prod", "url": "https://reponexus-portfolio.fly.dev/", "parity_gaps": ["SQLite volume persistence vs local file"]}
  ],
  "observability": {
    "dashboards": [{"name": "Fly logs", "url": "TODO: add link/runbook"}],
    "alerts": [{"name": "TODO", "url": "TODO", "status": "quiet"}]
  },
  "backlog_next_steps": [
    {"title": "Add explicit workflow state machine (no inferred step jumps)", "owner": "TODO", "eta_days": 2, "risk": "high"},
    {"title": "Fix owner/repo parsing for evidence fetching", "owner": "TODO", "eta_days": 1, "risk": "high"},
    {"title": "Fix SSE controller invalid state in repo analysis", "owner": "TODO", "eta_days": 2, "risk": "med"},
    {"title": "Unify package manager + resolve ESLint peer conflicts", "owner": "TODO", "eta_days": 2, "risk": "med"},
    {"title": "Add CI pipeline and document release gates", "owner": "TODO", "eta_days": 3, "risk": "med"}
  ],
  "risks": [
    {"risk": "Auto-grouping appears to start before user intent; trust erosion", "likelihood": "high", "impact": "high", "mitigation": "Implement explicit workflow state; ensure no background /api/curate/auto calls."},
    {"risk": "Evidence citations incorrect due to GitHub file fetch errors", "likelihood": "high", "impact": "high", "mitigation": "Fix owner/repo parsing; add tests + logging for evidence collection."},
    {"risk": "Install unreproducible due to conflicting lockfiles/peer deps", "likelihood": "med", "impact": "med", "mitigation": "Choose one package manager; align eslint/@typescript-eslint versions."}
  ],
  "todos": [
    {"item": "Commit stabilization changes and capture SHA/date", "owner": "TODO", "due": "TODO"},
    {"item": "Document workflow state machine and enforce gating", "owner": "TODO", "due": "TODO"},
    {"item": "Add CI pipeline (GitHub Actions?)", "owner": "TODO", "due": "TODO"}
  ]
}
```
